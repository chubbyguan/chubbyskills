# 社区贡献处理记录

整理日期：**2026-09-16**。本文根据公开 PR 说明记录处理建议，供维护者安排工作；不表示已完成代码审查、真实服务验证、合并、关闭或对外回复。执行前核对各 PR 的最新提交和 CI 状态。

## 待处理贡献

| PR | 公开说明里的改动 | 建议优先级与处理方式 | 合并前需要的证据 |
|---|---|---|---|
| [#1：安装依赖路由](https://github.com/chubbyguan/chubbyskills/pull/1) | 区分运行依赖安装与 Agent skill 注册；校验名称并调整依赖分组 | 优先处理。对照当前安装器吸收问题定义与仍适用的修改，避免把旧版安装假设直接带入 | 全仓库安装、指定 skill、未知名称非零退出、轻量任务不受无关重依赖阻塞；中英文命令一致 |
| [#3：Atlas Cloud 播客转录](https://github.com/chubbyguan/chubbyskills/pull/3) | 可选云端 ASR，保留本地默认；作者说明有 mock 测试，未执行计费转录 POST | 与 #5 一起评估 provider 接口；先保留待验证状态 | 显式选择云端、音频上传与费用说明、超时/失败处理、不因轮询重试重复提交；经授权的真实转录输出与本地回归 |
| [#5：MuAPI 播客转录](https://github.com/chubbyguan/chubbyskills/pull/5) | 可选托管后端、上传及轮询；作者说明未发起真实或计费请求 | 使用与 #3 相同的接口和证据要求，不为不同服务复制两套流程 | 除上述要求外，核对文件大小限制、上传与返回地址处理、时间戳和统一 Markdown；真实验证前不能标为已验证 provider |
| [#6：外部 cue-omni-reader 文档入口](https://github.com/chubbyguan/chubbyskills/pull/6) | 维护者披露利益关系，建议在 README 加可选外部 skill 安装入口；可能计费 | 先验证与现有 PDF、本地音频/视频路径相比的具体缺口；适合评估为可选集成，不进入首跑必需依赖 | 一条可复现的输入→Markdown→本库检索示例，说明上传范围、费用和维护归属；不把外部能力列成本仓库已提供功能 |

## 建议执行顺序

1. 对照 #1 完成安装入口和依赖回归；保留贡献归属。若当前实现采用不同方案，说明哪些问题已解决、哪些仍未解决。
2. 先验证现有本地播客转录，再确定 #3/#5 共用的 provider 约定和测试要求。mock 覆盖可以合入测试证据，但不能替代真实服务验收。
3. 对 #6 跑一条有明确缺口的集成示例。若没有验证或缺口不成立，记录理由后再决定是否收入集成目录。
4. 每项处理后更新下表，附对应提交、测试输出或公开操作链接。

## 执行记录

| PR | 已执行动作 | 验证证据 | 公开处理链接 |
|---|---|---|---|
| #1 | 0.11.1 已用当前安装器解决职责区分、完整目录名路由、未知名称拒绝、轻量依赖边界；没有直接合并旧 PR | `tests/test_install_skill.py`：全部 14 项路由、可搬迁产物、覆盖保护；感谢该 PR 提供的问题定义 | 本轮修复版；旧 PR 的关闭与说明尚未执行 |
| #3 | 待执行 | — | — |
| #5 | 待执行 | — | — |
| #6 | 待执行 | — | — |

任何对外评论、合并或关闭都是独立的维护动作，需由维护者实际执行后再记录。本文不自动发送消息，也不代表对第三方付费服务的采用承诺。

Issue [#4](https://github.com/chubbyguan/chubbyskills/issues/4) 的 MCP 启动失败已在 2.2.0 上复现；0.11.1 固定 SDK 1.30.0，并通过真实 stdio 初始化、6 个工具发现、搜索和读取。该验证不等于完整 MCP 协议一致性认证。
